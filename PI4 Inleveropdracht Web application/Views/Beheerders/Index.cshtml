@model IEnumerable<PI4_Inleveropdracht_Web_application.Models.Beheerder>

@{
    ViewBag.Title = "Index";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    @if (Session["BeheerderNaam"] != null)
    {
        <p> @Html.ActionLink("Create New", "Create") </p>
    }
    
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BeheerderNaam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BeheerderWachtwoord)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.BeheerderNaam)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.BeheerderWachtwoord)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.BeheerderId }) |
                    @Html.ActionLink("Details", "Details", new { id = item.BeheerderId }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.BeheerderId })
                </td>
            </tr>
        }

    </table>
</body>
</html>
